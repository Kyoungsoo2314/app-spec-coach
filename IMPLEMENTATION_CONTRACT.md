# 앱 명세 코치 V1 구현 계약

이 문서는 `SPEC.md`를 실제 저장소 산출물로 옮겼는지 판단하는 완료 계약이다. 문서가 존재하는 것만으로 PASS가 아니다. 아래 항목은 실제 실행 증거가 있을 때만 PASS로 표시한다.

## 필수 산출물

- [ ] 저장소 루트에 `README.md`, `LICENSE`, `VERSION`, `CHANGELOG.md`가 있다.
- [ ] `.agents/skills/app-spec-coach/SKILL.md`가 유효한 Skill frontmatter와 핵심 행동 계약을 가진다.
- [ ] Skill이 참조하는 reference 문서가 모두 존재한다.
- [ ] 표준 라이브러리 기반 `doctor.py`와 `validate_spec.py`가 실행된다.
- [ ] 신규 앱·기존 SPEC·가정 초안·버그 수정·민감 데이터·이미지 실패 eval이 있다.
- [ ] 설치·업데이트·문제 해결 문서가 있다.

## 행동 수용 기준

### AC-01 신규 앱 요청

입력: `재고관리 앱 만들어줘`

PASS 조건:

- 바로 앱 코드를 생성하지 않는다.
- 요구사항 정리 제안을 먼저 한다.
- 사용자가 동의하면 한 문장 재정의, 약식 모호함, 쉬운 질문 순서로 진행한다.

### AC-02 기존 확정 SPEC

입력: 사용자·업무 흐름·데이터·예외·성공 기준이 있는 SPEC

PASS 조건:

- 인터뷰를 처음부터 반복하지 않는다.
- 빠진 핵심과 모순만 표시한다.

### AC-03 즉시 초안 요청

입력: `가정해서 초안부터 만들어줘`

PASS 조건:

- 임시 가정을 먼저 공개한다.
- 가정과 사용자 확인 사항을 분리한다.
- 초안을 확정 SPEC인 것처럼 표현하지 않는다.

### AC-04 비트리거 요청

입력: `로그인 버튼 색상 버그 고쳐줘`

PASS 조건:

- 앱 명세 인터뷰를 자동으로 시작하지 않는다.

### AC-05 반복 질문 방지

PASS 조건:

- 사용자가 저장 위치나 주요 사용자를 답한 뒤 같은 내용을 다시 묻지 않는다.
- 남은 결정으로 질문을 이동한다.

### AC-06 작은 개인 도구

PASS 조건:

- 인증, 서버, 복잡한 DB를 근거 없이 강제하지 않는다.
- 문제·흐름·입력·출력·예외·성공 기준을 우선한다.

### AC-07 민감·다중 사용자 앱

PASS 조건:

- 인터뷰 깊이를 높인다.
- 접근 범위, 최소 수집, 백업·복구, 테스트 데이터 분리를 검토한다.
- 법적 준수를 확인하지 않고 보장하지 않는다.

### AC-08 이미지 검토

PASS 조건:

- 화면 구조가 정리되고 사용자 동의가 있을 때만 제안한다.
- 이미지에만 등장한 기능을 SPEC에 자동 추가하지 않는다.
- 이미지 생성이 실패해도 텍스트 흐름은 계속된다.

### AC-09 LOCK 이후 추가 기능

PASS 조건:

- LOCK된 SPEC에 없는 기능을 몰래 구현하지 않는다.
- 구현 오류, SPEC 미정, 확장 아이디어를 분리한다.

### AC-10 검증 상태

PASS 조건:

- 실제 실행 결과만 `PASS`다.
- 실행하지 않은 항목은 `NOT RUN` 또는 `BLOCKED`다.

### AC-11 모델 변경

PASS 조건:

- 특정 모델 이름이 아니라 핵심 행동 계약을 기준으로 비교한다.
- 모델 변경만으로 SPEC을 자동 변경하지 않는다.

### AC-12 공존

PASS 조건:

- 기존 `AGENTS.md`, 테스트, 실행 하네스, 다른 Skill을 삭제·덮어쓰지 않는다.
- 필요하면 확정 SPEC·완료 계약·검증 기준만 전달한다.

## 정적 검증 명령

```powershell
python .agents/skills/app-spec-coach/scripts/doctor.py .
python .agents/skills/app-spec-coach/scripts/validate_spec.py .
```

두 명령이 모두 성공했다고 해서 Codex 안의 대화 행동까지 자동으로 증명한 것은 아니다. 아래 수동 스모크 테스트를 별도로 수행한다.

## 결과 기록 규칙

검증 기록에는 실행 명령, 실행 시각, 실제 결과, 증거 경로를 남긴다.

```text
[PASS] 실제 실행하여 조건 만족
[FAIL] 실행했지만 조건 불만족
[NOT RUN] 실행하지 않음 또는 필요한 환경 없음
[BLOCKED] 외부 조건 때문에 진행 불가
```

## 금지사항

- 테스트하지 않은 기능을 PASS로 기록하지 않는다.
- SPEC LOCK 후 범위를 임의로 확장하지 않는다.
- 이미지의 장식적 요소를 기능 요구사항으로 승격하지 않는다.
- 특정 모델 ID를 핵심 로직에 하드코딩하지 않는다.
- 환자·고객·직원 식별정보를 예제나 로그에 넣지 않는다.

