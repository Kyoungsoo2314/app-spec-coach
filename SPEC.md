---
status: locked
spec_version: 1.4
product: app-spec-coach
product_form: codex-skill
---

# 앱 명세 코치 — LOCKED SPEC v1.4

## 1. 제품 정의

앱 명세 코치는 앱 개발 경험이 적은 사용자의 평범한 설명을 대화로 구체화하여, 구현 가능한 `SPEC`과 검증 가능한 완료 계약으로 바꾸는 Codex Skill이다.

핵심 문장은 다음과 같다.

> 사용자가 “앱 만들어줘”라고 말해도, 요구사항이 충분하지 않으면 바로 코딩하지 않고 필요한 결정을 쉬운 질문으로 함께 정리한다.

## 2. 핵심 사용자와 문제

주 사용자는 비개발자·앱 개발 초보자이며, 바이브코딩으로 자신의 업무를 앱으로 바꾸려는 실무자를 포함한다.

초보자의 짧은 요청에는 사용자, 문제, 현재 업무, 입력 데이터, 결과, 업무 순서, 상태·예외, 화면, 저장 방식, 완료 기준이 빠져 있을 수 있다. Skill은 이 빈칸을 사실처럼 추측하지 않는다.

## 3. 책임 범위

```text
요구사항 명확화
→ SPEC 작성
→ 구현 계약 작성
→ 구현 전 사용자 승인
→ 실제 결과와 SPEC 비교
```

이 Skill은 범용 실행 하네스, IDE, 자체 LLM 서버, 자체 인증 서비스가 아니다.

## 4. 기본 상태 흐름

```text
REQUEST
  ↓
SUFFICIENCY CHECK
  ↓
INTERVIEW (필요한 경우)
  ↓
AMBIGUITY REDUCTION
  ↓
WORKFLOW / MENU / SCREEN REVIEW
  ↓
SPEC DRAFT
  ↓
VISUAL AWARENESS
  ↓
MENU / SCREEN / DASHBOARD BRIEF
  ↓
FINAL VISUAL OFFER
  ↓
USER APPROVAL → IMAGE
  ↓
FINAL QUESTIONS
  ↓
SPEC LOCK
  ↓
IMPLEMENTATION APPROVAL
  ↓
IMPLEMENT
  ↓
VERIFY
```

## 5. 개입 계약

신규 앱 요청이 충분히 구체적이지 않으면 다음 의미의 제안을 먼저 한다.

> 아직 사용자, 업무 흐름, 저장할 정보와 완료 기준이 충분히 정해지지 않았습니다. 바로 만들기 전에 필요한 내용을 저와 간단히 정리해드릴까요?

사용자에게는 다음 세 경로를 제공한다.

1. 같이 정리하기
2. 기존 명세 검토하기
3. 합리적인 가정으로 초안부터 만들기

사용자가 즉시 구현을 선택하면 임시 가정과 최소 완료 기준을 먼저 공개하고, 확정 SPEC인 것처럼 말하지 않는다.

## 6. 인터뷰 계약

- 기술 선택이 아니라 실제 업무 상황을 질문한다.
- 기본 질문 수는 한 번에 1~3개이며, 새 주제의 첫 질문은 가능하면 하나다.
- 답한 내용을 STATE에 누적하고 같은 질문을 반복하지 않는다.
- 결과를 크게 바꾸는 미확정 사항을 우선한다.
- 인터뷰가 시작되면 현재 모호함을 `약 N%`로 표시한다.

모호함은 다음 100점 가중치를 사용한다.

| 항목 | 가중치 |
|---|---:|
| 해결 문제 | 10 |
| 주요 사용자·사용 시점 | 8 |
| 현재 업무·불편 | 8 |
| 입력·기록 데이터 | 8 |
| 결과·판단 정보 | 8 |
| 핵심 업무 흐름 | 12 |
| MVP·비목표 | 8 |
| 메뉴·화면·이동 | 8 |
| 상태·예외 | 7 |
| 성공 기준 | 8 |
| 데이터 보존 | 5 |
| 인증·보안 | 4 |
| 계산·업무 규칙 | 3 |
| 운영 환경 | 3 |

확인도는 `0`, `0.5`, `1`만 사용한다. 필요한 없음이 확인된 항목은 확인도 `1`로 처리한다.

## 7. 전환 계약

- 약 40% 이하: 메뉴·화면 및 시각 검토를 제안할 수 있다.
- 약 30% 이하: SPEC 초안을 작성할 수 있다.
- 약 15% 이하: 핵심 확인 후 LOCK을 검토할 수 있다.
- 숫자가 낮아도 핵심 사용자 판단이 미확정이면 LOCK하지 않는다.

## 8. 데이터·위험 계약

업무 데이터 앱은 하드코딩 샘플이나 새로고침 시 사라지는 가짜 CRUD를 완료로 보지 않는다. 입력 → 실제 저장 → 재조회 → 수정 흐름을 확인한다.

개인정보, 의료정보, 중요한 금액, 결제, 다중 사용자, 외부 네트워크, 파일 업로드가 있으면 인터뷰 깊이를 높이고 인증·권한·최소 수집·보호 경계·백업·복구·테스트 데이터 분리를 검토한다. 법률 준수를 자동으로 단정하지 않는다.

## 9. 화면·이미지 계약

메뉴는 업무 흐름에서 도출한다. 입력 화면만 있고 저장 후 찾기·상세·수정·삭제 상태가 없으면 화면 설계가 불완전한 것으로 표시한다.

초보자·처음 사용하는 사용자는 이미지 기능을 먼저 요청하지 않아도 된다. 업무 흐름과 최소 화면이 잡히면 시각 검토가 가능하다는 사실을 먼저 안내하되, 바로 이미지를 생성하지 않는다. 메뉴·화면·대시보드·필수 정보 논의를 끝낸 뒤 최종 시안을 제안하고, 사용자가 동의한 경우에만 현재 호출 가능한 이미지 capability를 같은 턴에 사용한다. 사용자가 거부하면 `SKIPPED`, capability가 없거나 호출에 실패하면 `NOT RUN`을 기록한다. 특정 이미지 모델 ID는 고정하지 않는다.

이미지는 디자인 참고자료이며, 이미지에만 등장한 버튼·카드·통계·필드·상태는 요구사항이 아니다. 이미지와 SPEC이 충돌하면 SPEC을 따른다. 이미지 생성 실패는 인터뷰·SPEC 진행을 중단시키지 않는다.

## 10. SPEC LOCK 계약

다음 조건을 모두 만족해야 한다.

- 핵심 사용자와 문제 확인
- 입력과 결과 연결
- 핵심 업무 흐름 연결
- 주요 예외 결정
- MVP와 비목표 구분
- 성공 기준이 검증 가능
- 구현 형태 결정
- 메뉴·화면·대시보드·필수 정보 논의 완료
- 최종 시안 제안 및 동의·거부 또는 `NOT RUN` 사유 기록
- 사용자 판단이 필요한 핵심 미확정 사항 없음
- 모호함 약 15% 이하

LOCK된 SPEC에 없는 기능은 임의로 추가하지 않는다. 구현 오류는 코드 수정으로, SPEC 미정은 사용자 확인으로, 추가 아이디어는 확장 후보로 분류한다.

## 11. 구현·검증 계약

SPEC LOCK 뒤에도 자동으로 구현을 시작하지 않고 다음 의미로 승인받는다.

> 명세가 구현 가능한 수준으로 정리됐습니다. 이 SPEC을 기준으로 실제 구현까지 진행해드릴까요?

검증 결과는 다음 네 상태만 사용한다.

- `PASS`: 실제로 실행하여 조건을 만족함
- `FAIL`: 실제 실행했으나 조건을 만족하지 못함
- `NOT RUN`: 실행하지 못했거나 실행하지 않음
- `BLOCKED`: 외부 조건 때문에 진행할 수 없음

`NOT RUN`과 `BLOCKED`를 `PASS`로 바꾸지 않는다.

모든 코칭 응답은 요약만 남기고 끝내지 않는다. 아직 결정이 남아 있으면 구체적인 다음 질문을 하고, SPEC 1.0 확정 전이면 확정 여부를 묻고, SPEC LOCK 후 구현 전이면 명세 코칭 완료와 구현 승인 질문을 명시한다. 구현·검증이 끝난 경우에는 검증 상태와 남은 조치를 함께 표시한다.

핵심 범위가 정리되고 SPEC 1.0 확정을 묻기 직전에는 `더 원하시는 기능이 있나요?`를 한 번 확인한다. 사용자가 추천을 원하면 관련 확장 후보를 최대 2~3개 제시하되, 사용자가 선택하기 전에는 SPEC에 자동으로 추가하지 않는다. SPEC LOCK 후 제안된 기능은 확장 후보 또는 새 버전 변경으로 분류한다.

## 12. 모델·배포 계약

- 특정 설계 모델이나 이미지 모델을 핵심 규칙에 고정하지 않는다.
- 사용자가 선택한 모델 설정은 존중한다.
- 모델 변경은 문장 일치가 아니라 행동 계약 eval로 검증한다.
- V1 배포 본체는 GitHub Skill이다.
- Plugin은 후속 배포 채널이며 V1 완료 조건이 아니다.
- 독립 Windows/Mac 앱, 자체 로그인, 자체 API 서버, 자체 이미지 서버는 V1 비목표다.

## 13. V1 성공 기준

1. GitHub 저장소에서 Skill 설치가 가능하다.
2. 새 Codex 세션에서 Skill이 발견된다.
3. “재고관리 앱 만들어줘”에 즉시 코드 대신 코칭 제안을 한다.
4. 동의 후 쉬운 선택형 인터뷰와 모호함을 표시한다.
5. 답한 내용을 반복 질문하지 않는다.
6. 업무 흐름에서 최소 메뉴·화면을 도출한다.
7. 구조화된 SPEC과 구현 계약을 만든다.
8. 실제로 실행하지 않은 검증을 PASS로 보고하지 않는다.
9. 초보자에게 이미지 시각 검토 기능을 먼저 안내한다.
10. 메뉴·화면·대시보드·필수 정보 논의 뒤 최종 시안을 제안하고, 동의한 경우에만 실제 이미지를 보여준다.
11. 이미지 capability 없이도 핵심 흐름이 완료된다.
12. 특정 모델 ID가 핵심 동작에 하드코딩되지 않는다.
13. 코칭의 각 종료 지점에서 현재 단계와 다음 행동 또는 완료 상태가 사용자에게 명확히 표시된다.
14. 핵심 범위 확정 직전에 추가 기능을 선택적으로 확인하고, 선택하지 않은 추천 기능을 SPEC에 자동 추가하지 않는다.
